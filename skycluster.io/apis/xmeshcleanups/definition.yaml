apiVersion: apiextensions.crossplane.io/v1
kind: CompositeResourceDefinition
metadata:
  name: xmeshcleanups.skycluster.io
spec:
  group: skycluster.io
  names:
    categories:
    - crossplane
    kind: XMeshCleanup
    plural: xmeshcleanups
  versions:
  - name: v1alpha1
    referenceable: true
    schema:
      openAPIV3Schema:
        description: XMeshCleanup is the Schema for the XMeshCleanup API.
        properties:
          spec:
            description: XMeshCleanupSpec defines the desired state of XMeshCleanup.
            properties:
              clusterNames:
                items:
                  type: string
                type: array
              localCluster:
                properties:
                  podCidr:
                    type: string
                  serviceCidr:
                    type: string
                type: object
            type: object
          status:
            description: XMeshCleanupStatus defines the observed state of XMeshCleanup.
            properties:
              clusters:
                items:
                  properties:
                    nameRef:
                      type: string
                    status:
                      type: string
                  type: object
                type: array
              log:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
